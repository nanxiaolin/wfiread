function onexporttype(event, obj)
    %global h_mainfig;
    
    %userdata = get(h_mainfig, 'userdata');
    %sel = get(userdata.h_exporttype, 'value');
    
    %disp(sel);
    %switch sel
    %    case 1      % export as view; switch off the 'selection only' box
    %        set(userdata.h_exportselection, 'enable', 'off');
    %    case 2      % export as movie file; switch on 'selection only' box
    %        set(userdata.h_exportselection, 'enable', 'on');
    %end
            
return
